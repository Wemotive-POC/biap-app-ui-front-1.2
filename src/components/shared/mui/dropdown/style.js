import { makeStyles } from "@mui/styles";

const style = makeStyles({
  dropdown_text: {
    fontSize: "14px !important",
    fontWeight: 400,
    lineHeight: 1.5,
    color: "#606161",
    margin: 0,
  },
});

export default style;
